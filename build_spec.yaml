version: 0.0.1
steps:
  - name: Build Docker Image
    shell: bash
    commands:
      - docker build -t myapp:latest .
  - name: Push Docker Image to OCR
    shell: bash
    commands:
      - echo ${{SECRET.AUTH_TOKEN}} | docker login -u 'axwspk2pkmor/zaid.elmouaddibe-etu@etu.univh2c.ma' --password-stdin mad.ocir.io
      - docker tag myapp:latest mad.ocir.io/axwspk2pkmor/repo:latest
      - docker push mad.ocir.io/axwspk2pkmor/repo:latest
